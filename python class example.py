first_prompt = input("The weather is sunny today, type Y to go outside and type N to stay inside")
#first_prompt = Y or the first_prompt = N

if (first_prompt == "Y"):
    second_prompt = input("You went outside and tripped, type Y to call an ambulance and type N to lay down")
        if (second_prompt == "Y"):
        #tell the user to do something
    else:
        #tell the user to do something
else:
    second_prompt = input("You stayed home and played minecraft, type Y to order food and type N to drink some water")
    if (second_prompt == "Y"):
        #tell the user to do something
    else:
        #tell the user to do something